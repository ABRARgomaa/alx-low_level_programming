#include <stdio.h>
/**
 * main - Entry Point
 * Return: Always(0)
 */
int main(void)
{
	char ch = 'a'
	while (ch <='z')
	{
		putchar(ch);
		ch++;
	}
	char c = 'A'
		while (c <= 'Z')
	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
	
